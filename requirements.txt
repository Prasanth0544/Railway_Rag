# Railway RAG Assistant — Python Dependencies

# ── Web Framework ──────────────────────────────────────
fastapi
uvicorn[standard]

# ── LangChain Core ────────────────────────────────────
langchain
langchain-core
langchain-community

# ── LLM Providers ─────────────────────────────────────
langchain-google-genai          # Gemini API (cloud LLM + embeddings)
langchain-openai                # OpenAI-compatible (for LM Studio local server)

# ── Vector Store ──────────────────────────────────────
langchain-chroma
chromadb

# ── Data Processing ───────────────────────────────────
pandas
python-dotenv
python-multipart                # For FastAPI file uploads (image/PDF)

# ── Multi-Modal (Gemini Vision) ───────────────────────
google-generativeai             # Native Gemini SDK for image/PDF/audio processing
Pillow                          # Image processing for multi-modal uploads (PNG/JPG/WEBP)

# ── Optional: Offline/Local Embeddings (LM Studio mode)
sentence-transformers         # For USE_LOCAL_EMBEDDINGS=true (offline, no rate limits)
