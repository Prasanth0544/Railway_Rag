# Railway RAG Assistant — Python Dependencies

# ── Web Framework ──────────────────────────────────────
fastapi
uvicorn[standard]

# ── LangChain Core ────────────────────────────────────
langchain
langchain-core
langchain-community

# ── LLM Providers ─────────────────────────────────────
langchain-google-genai          # Gemini API (cloud LLM + embeddings)
langchain-openai                # OpenAI-compatible (for LM Studio local server)

# ── Vector Store ──────────────────────────────────────
langchain-chroma
chromadb

# ── Data Processing ───────────────────────────────────
pandas
python-dotenv
python-multipart                # For FastAPI file uploads (image/PDF)
pydantic-settings               # Config validation (app/config.py)

# ── Multi-Modal (Gemini Vision) ───────────────────────
google-genai                    # New unified Gemini SDK (replaces deprecated google-generativeai)
Pillow                          # Image processing for multi-modal uploads (PNG/JPG/WEBP)

# ── Offline/Local Embeddings (matches 384-dim ChromaDB vector database) ────
sentence-transformers==3.4.1

# ── Streamlit UI (local dev only) ────────────────────────────────────────────
# streamlit              # ← DISABLED on Render (saves ~150MB RAM).
#                          Install manually for local Streamlit UI:
#                          pip install streamlit
